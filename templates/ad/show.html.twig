 {% extends "base.html.twig" %}

 {% block body %}
 
  <div class="ad-title" style="background-image:url({{ ad.coverImage }})"></div>
      <div class="container">
          <h1>{{ ad.title }}</h1>
          <p>{{ ad.introduction }}</p>
          <p class="h2">
              <strong>{{ad.rooms}} chambres  </strong>pour <strong>{{ad.price}}&euro;</strong>
            </p>
            <a href="#" class="btn btn-primary">Reserver</a>
      </div>
      <hr>
      <div class="container mt-5">
          <div class="row">
              <div class="col-md-8 mb-3">
                  {{ad.content | raw}}
                  {#//////////////  #}
                  <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                            <ol class="carousel-indicators">
                                {% for image in ad.images %} 
                                     <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" 
                                {% if loop.first %}  class="active" {% endif %}></li>
                                        {% endfor %}
                                </ol>
                            <div class="carousel-inner">
                                {% for image in ad.images %} 
                                  <div class="carousel-item {% if loop.first %} active {% endif %}">
                                    <img src="{{image.url}}"  style="height: 300px;width: 80%; display:block ;"{#{{https://via.placeholder.com/200x200}} #} class="d-block w-100" alt="ad">
                                  
                                       <div class="carousel-caption d-lg-none d-md-block">
                                             <h5>{{ad.title}}</h5>
                                             <p>{{image.caption}}</p>
                                      
                                           </div>
                                   </div>
                                {% endfor %}
                            </div>
                            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                <span class="sr-only">Previous</span>
                            </a>
                            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                <span class="sr-only">Next</span>
                            </a>
                            </div>

                  {#/////////////////  #}
              </div>
              <div class="col">
                  <div class="row">
                      <div class="col-4">
                           <img src="https://via.placeholder.com/64x64" alt="avatar">
                      </div>
                      <div class="col">
                          <h3>taoufik ahjam</h3>
                          <span class="badge badge-primary">
                              3 annonces
                          </span>
                      </div>  
                  </div>
                   <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Numquam, ut!</p>
                   <p>Ut amet aliquam eligendi vero, excepturi necessitatibus rerum doloribus exercitationem.</p>
                   <p>Consequatur sequi ea iure ipsam soluta blanditiis, corporis laborum dolores.</p>
                   
              </div>
          </div>
      </div>

   
 
 {% endblock %}